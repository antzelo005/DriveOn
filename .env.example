# Pages URL and base are derived from GITHUB_REPOSITORY in CI.
# Local defaults live in config/github-pages.json. No domain variable is needed.
# Empty = simulated submission; no data leaves the browser.
VITE_LEAD_ENDPOINT=
